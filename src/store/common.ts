
const state = {
    loading: true
}

const getters = {
}

const mutations = {
  setLoading(state, value) {
    state.loading = value
  },
}

const actions = {
}

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions
}